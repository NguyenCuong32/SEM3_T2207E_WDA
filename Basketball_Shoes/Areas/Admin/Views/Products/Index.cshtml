@model IEnumerable<Basketball_Shoes.Models.Product>

@{
    ViewData["Title"] = "Index";
}

@* <h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p> *@
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
<div class="container mt-3">
    <table class="table table-hover">
        <thead>
                <p>
                    <a class="btn btn-secondary" href="/Admin/Products/Create">Create New</a>
                </p> 
            <tr>
                <th>Id</th>
                <th>Thumbnail</th>
                <th>Description</th>
                <th>Price</th>
                <th>Sale</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <td>@item.id</td>
                    <td style="height:100px">
                        <img src="@item.thumbnail" style="height:100%"/>
                    </td>
                    <td>@item.description</td>
                    <td>@item.price</td>
                    <td>@item.sale</td>
                    <td>
                        <a class="btn btn-danger" href="">Delete</a>
                        <a class="btn btn-warning">Details</a>
                        <a class="btn btn-primary mt-2">Edit</a>

                    </td>
                </tr>
            }
            
        </tbody>
    </table>
</div>
</body>
</html>
